/marks/:id:
    get:
      tags:
      - Mark
      summary: Returns a list of users.
      description: Optional extended description in CommonMark or HTML.
      responses:
        '200':    
          description: A JSON comment and its marks
          content:
            application/json:
              schema: 
                type: object
                items: 
                  type: string